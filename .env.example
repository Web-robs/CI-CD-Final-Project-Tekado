#
# Required (backend)
#
JWT_SECRET=8db0d104ef5d244c4ef1f4ad6c79f6df96690a19951daa54e37ccc79b1e954a8

#
# Required for Docker Compose stack
#
POSTGRES_USER=tekado_user
POSTGRES_PASSWORD=tekado_pass
POSTGRES_DB=tekado_db
POSTGRES_HOST_PORT=5433

# Optional: only needed when running backend outside Docker Compose
DATABASE_URL=postgresql://tekado_user:change_me@localhost:5432/tekado_db?schema=public

#
# Required (frontend build-time for CRA/CRACO)
#
REACT_APP_API_BASE_URL=http://localhost:8000/api

#
# Optional services / integrations
#
MONGO_URI=
WEAVIATE_HOST=
WEAVIATE_API_KEY=
GOOGLE_AI_API_KEY=

# SMTP (email verification)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="Tekado Store <no-reply@example.com>"
